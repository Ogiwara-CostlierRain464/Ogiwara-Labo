#ifndef LABO_CAMERA_H
#define LABO_CAMERA_H

namespace labo::render{
class Camera{
public:
  explicit Camera(
    float windowX,
    float windowY,
    float fov);

private:

};
}

#endif //LABO_CAMERA_H
